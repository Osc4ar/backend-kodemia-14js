const entrada = [1, 2, 3, 4, 5];

function maximo(entrada) {
    const resultado = 'lo que sea mi resultado';
    // aqui va su codigo

    return resultado;
}

function division(a, b) {
    return 'resultad';
}

const resultado = maximo(entrada);
console.log(resultado);